import 'package:flutter/material.dart';

class Fragen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
            title: Text(
              'Fragen',)
        )
    );
  }

}